import { processBlock } from "../src/block-processor";

describe("BlockProcessor - ...", () => {
  // beforeEach
  // mocks

  test("...", async () => {
    // processBlock()
    expect(true).toStrictEqual(true);
  }, 10000);
});
